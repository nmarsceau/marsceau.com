---
layout: layouts/simple.njk
permalink: "/postroll{% if pagination.pageNumber > 0 %}/{{ [ pagination.pageNumber, 1] | sum }}{% endif %}/index.html"
title: Postroll | Nick Marsceau
eleventyExcludeFromCollections: true
pagination:
  data: postroll
  size: 20
  alias: postrollPage
  reverse: true
---
{% css %}
.postroll {
	list-style-type: none;
    padding-inline-start: 0;
}

.postroll cite {
	font-size: 1.2rem;
	display: block;
}

.postroll .author, .postroll .readOn {
	font-size: 0.8rem;
	display: block;
}
{% endcss %}
<h1>Postroll</h1>
<p><a href="https://slashpages.net/#postroll"><small>What is a postroll?</small></a></p>
<ul class="postroll">
    {%- for post in postrollPage -%}
        <li>
            <cite><a href="{{ post.url }}">{{ post.name }}</a></cite>
			<span class="author">By: {{ post.author }}</span>
			<span class="readOn">
				Read on:&nbsp;
				{%- if post.readOn == 'Unknown' -%}
					Unknown
				{%- else -%}
					<time datetime="{{ post.readOn }}">{{ post.readOn | toLocaleDateString }}</time>
				{%- endif -%}
			</span>
        </li>
    {%- endfor -%}
</ul>
{% include "layouts/components/pagination.njk" %}
